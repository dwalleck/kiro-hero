---
title: Troubleshoot the failure
description: Work from symptom to cause with the agent.
sidebar:
  order: 3
---

import { Aside } from '@astrojs/starlight/components';

**Goal:** start from the symptom and use the agent to trace it back to a root cause
in code you didn't write — the real-world skill this whole workshop builds toward.

<Aside type="tip" title="Your turn">
TODO: participants hit the failure, capture the symptom (log line / status code /
stack trace), and drive the agent from that symptom toward the cause. Emphasise
forming and checking hypotheses rather than asking the agent to "just fix it."
</Aside>

<Aside type="note" title="Checkpoint">
TODO: the bug is identified and fixed, the deploy is healthy, and participants can
articulate *how* they found it — not just that it's green now.
</Aside>

<Aside type="tip" title="Stretch goal">
TODO (fast finishers): add a hook or a test that would have caught this failure
before deploy — closing the loop back to the hooks lesson in Act 1.
</Aside>
