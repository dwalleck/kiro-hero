---
title: Plan the change
description: Turn understanding into an explicit plan before writing code.
sidebar:
  order: 3
---

import { Aside } from '@astrojs/starlight/components';

**Goal:** produce an explicit plan for the change before any code is written. This
is the spine of the whole workshop — planning isn't a feature you toggle on, it's
the difference between a change that holds and one that silently breaks.

<Aside type="note" title="Instructor demo — watch it flail">
TODO: hand the agent the change with *no* plan. Let it wander or jump straight to
editing the obvious file. Run it, exercise the new path, and surface the runtime
miss — "it compiles, it looks done, it's wrong."
</Aside>

<Aside type="tip" title="Your turn — make a plan first">
TODO: participants ask the agent to *locate every place the existing variants
appear* and produce a short plan / spec before editing. Kiro specs live under
`.kiro/specs/`; the exact prompts and acceptance criteria depend on the chosen app.
</Aside>

<Aside type="note" title="Checkpoint">
TODO: everyone has a written plan enumerating the touch points (registry, dispatch,
validation, serialization, test fixture — whatever the real set turns out to be).
</Aside>
