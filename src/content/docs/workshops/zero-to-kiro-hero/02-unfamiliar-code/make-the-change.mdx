---
title: Make the change
description: Execute against the plan until the done-signal fires and holds.
sidebar:
  order: 4
---

import { Aside } from '@astrojs/starlight/components';

**Goal:** add the new variant by executing the plan, and prove it holds with a
fast, local done-signal.

<Aside type="tip" title="Your turn">
TODO: execute the plan with the agent. Touch each enumerated place. Run the
done-signal — a test goes green, or the new variant returns the right shape when
exercised. Not "looks plausible": the path actually works.
</Aside>

<Aside type="note" title="Checkpoint">
TODO: the new variant works end to end and the existing ones still pass.
`git checkout <act2-done>` is the snapshot for anyone who fell behind.
</Aside>

<Aside type="tip" title="Stretch goal">
TODO (fast finishers): encode the convention you just followed as a steering file,
start a fresh agent session, and confirm it now produces the variant correctly
*unprompted*. This closes the loop back to the steering lesson in Act 1.
</Aside>
